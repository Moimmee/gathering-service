apiVersion: v1
kind: Service

metadata:
  name: gathering-service

spec:
  type: NodePort
  selector:
    app: gathering-app
  ports:
    - port: 9092
      targetPort: 8080
      protocol: TCP
      nodePort: 30002